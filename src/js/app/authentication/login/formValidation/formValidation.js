(function(window,angular){
    angular.module('authentication.login.formValidation',[])
        .directive('formValidation',[function(){
            return {
                restrict:'A',
                link:function(scope,el,attrs){
                    
                }
            }
        }])
})(window,window.angular);